//
// Created by stefan on 24.07.2023.
//
#pragma once

#include <QWidget>

struct QMenu : QWidget {
  explicit QMenu(QWidget* = nullptr) {}
  auto addAction(QAction*) const {
    // empty on purpose
  }
  auto exec(QPoint const&) const {
    // empty on purpose
  }
};
