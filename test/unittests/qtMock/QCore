//
// Created by stefan on 24.07.2023.
//

#pragma once

struct Qt {
  enum QColor {
    blue,
    red
  };
  using GlobalColor = QColor;
  enum MouseButton {
    RightButton,
    LeftButton,
    MiddleButton
  };
};
